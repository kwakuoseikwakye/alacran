// Brand values in one place — the license gate, landing page, and packaging all
// read the same name. Name is FINAL (Alacrán); price + checkout are still
// placeholders until Lemon Squeezy is set up (LAUNCH.md open decisions). The
// packaging script (scripts/package-macos.sh) has its own APP_NAME knob — keep
// the two in sync.
export const APP_NAME = "Alacrán" // scorpion — final product name
export const PRICE_LABEL = "$29/mo · 14-day free trial" // TODO: final price + trial
export const CHECKOUT_URL = "https://your-store.lemonsqueezy.com/buy/REPLACE-ME" // TODO: LS checkout URL
