// PLACEHOLDER branding — swap these once the product name / price / Lemon
// Squeezy checkout are finalized (LAUNCH.md open decisions). Kept in one place
// so the license gate, landing page, and packaging all read the same values.
// The packaging script (scripts/package-macos.sh) has its own APP_NAME knob —
// keep the two in sync when you rename.
export const APP_NAME = "AI Company Panel" // TODO: final product name
export const PRICE_LABEL = "$29/mo · 14-day free trial" // TODO: final price + trial
export const CHECKOUT_URL = "https://your-store.lemonsqueezy.com/buy/REPLACE-ME" // TODO: LS checkout URL
